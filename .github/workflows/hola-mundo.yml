name: Hola Mundo DevOps 2025

on:
  workflow_dispatch:

jobs:
  saludar:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Ejecutar script
        run: python script.py
